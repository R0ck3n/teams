{% extends 'base.html.twig' %}

{% block title %}Teams Builder
{% endblock %}

{% block body %}
	<div class="d-flex gap-3">
		<div class="d-flex flex-column gap-5 w-75">
			{{ include('teams/_form.html.twig') }}
			{{ include('teams/show.html.twig') }}
		</div>
		{{ include('peoples/_form.html.twig') }}
	</div>
{% endblock %}
